# E11 validation report — verdict: **PASS** (zero BLOCKING; 3 MINOR ride as a punch list)

Scanned ground: all five work orders + `chunks.md` in `/Users/mitt/dev/projects/phi-guardrails/plan/04-epics/E11-src-inventory-full-artifact/`, roadmap E11 row + M2 exit, ledger rows 76–80/147–151/630–667, constitution, E10 frozen digest (checkpoint GREEN @ `b994a3e`, 230/230 — matches the cut's "230 accepted at cut"), D-81/D-80/D-79/D-45/D-25 decision-log entries, Q-38 sheet entry (Status: RULED — D-81), and the committed sources every "verbatim" digest block claims to quote.

## Checklist results

**1. Mechanical (scripted — `mech.py`, output below): PASS.** 5 work-order files scanned (nonzero asserted). Every `depends-on` chunk exists; header deps match table deps match ledger deps one-to-one (C01 —, C02 C01, C03 —, C04 C01+C03, C05 C02+C04). [P] pair C01/C03 manifests fully disjoint (SDK file pair vs kit file pair). LOC 100+120+140+140+90 = 590 exactly as claimed; each chunk inside the 50–150/300 band; the 5-vs-"~4" chunk deviation is justified in `chunks.md`. Every chunk has a VERIFY with the verify command; C05 carries both instruments; ledger verify lines exist for all five.

**2. Self-containment (sampled C04 — most complex — C05, and C02): PASS.** Every "verbatim, current body" quote was diffed against committed source and is byte-accurate: `PGRRegistry class>>fromConfiguration:` (C02), `PCKKit` pipeline + `architectureSpecFor:layerMap:packages:` (C04), `guardrails.ston` (C05), all of `PCKArtifactBlockM1FormTest` incl. its scheduling class comment (C05), `PGRRegistryTest>>artifactWithKitsFragment:` and the `BaselineOfPGRScratchGrouped` role expansions (C02). All SDK spellings used are real: `PGRRegistrationSpec name:kind:check:`/`missing:kind:reason:`, `PGRVerdict green`/`redFindings:`, `PGRFinding target:message:rationale:`, `PGRConfiguration` readers incl. `srcPath`/`exemptPackageNames`, `PGRScratchSpecKit` `#echoRoles`. No work order says "see" another document; no interface is used before its introducing chunk (C04/C05 re-inline C01's frozen surface).

**3. Tests are real: PASS.** All 25 skeletons are given/when/then behavior assertions: C01's aliasing/fresh-copy/nil-legality tests would each fail under a plausible wrong implementation (the `respondsTo: #at:` denial pins a deliberate absence that is itself the D-81 contract); C02's echo-kit tests read handoff content off spec reasons; C03's fixture pair asserts verdict color, finding arity, and targets over scratch roots; C04's delegation witness asserts pairwise spec-list equivalence across the two doors; C05's pins assert exact key sets and spec-name sequences against the on-disk artifact and are explicitly ordered to fail first against the still-M1 artifact.

**4. Epic coverage: PASS.** Roadmap assigns ch. 7 §7.5 (→ C03 check contract, C04 registration/missing arm, C05 artifact) and ch. 4 §4.4 (→ C05 self-hosted map, verbatim per spec); R-38 full form → C05. P-NO-DEAD-SRC's three ch.-9-named legs land under the exact names and the exact class ch. 9 names (`testRedOnStrayDirectory`/`testGreenOnCleanRoot` in C03, `testMissingWithoutSrcKey` into `PCKSrcInventoryCheckTest` at C04). Exit checkpoint enumerates every chunk's suite; the 12-registration arithmetic (2 lint + 2 arch + 7 behavioral + 1 meta) and the 21-src-directory claim (20 role-covered packages + `BaselineOfPhiGuardrails`, verified by `ls src/`) both check out; net-new 20 = 6+3+5+5+1.

**5. Amended-surface completeness (scripted — `amended.sh`, 237 committed files scanned, nonzero asserted): PASS, table diff empty.** Every committed `src/` file referencing `guardrails.ston` = exactly C05's table (pin test · `PGRConfigurationTest` scratch · `PGRGateTest` decoy · `PCKNoSkippedTestsMetaRuleTest` comment; rest is docs/plan prose). Zero committed files assert a 10-registration count (the one `grep` hit is `PCKKit`'s class comment, no count). `fromConfiguration:` callers = C02's eight-file list exactly. `registrationsFrom:` kit-relevant callers = C04's list. Zero committed `src/` references to `registrationsFrom:environment:`; the only `PCKSrcInventoryCheck` src reference is a comment (see MINOR-3). The only accepted-test-file amendment is the scheduled `PCKArtifactBlockM1FormTest` (its own class comment: "E11 amends the M1-form assertions by schedule").

**6. Intent attribution (scripted sweep + spot-checks): PASS.** D-81's ruling text, veto quote, growth path, and additive-landing terms are quoted accurately (decision-log 2743–2782). D-25's "pairwise disjoint … jointly cover" backs the full-inventory concatenation claim verbatim. The accepted core comment "the walk that consumes it judges that at run time" exists verbatim (`PGRConfiguration.class.st:138`), and C03's red-not-missing-not-green ruling for a nonexistent root is a forced move from cited ground (§1.5 reserves missing for absent parameters; the frozen verdict surface offers a running check only green/red; family-7 loudness) — attributed, not invented. B-28 (4) exists in `backlog.md` and says what the orders claim. Q-38 is RULED—D-81 on the sheet; no open question rides the epic.

## Findings (all MINOR — punch list, no re-validation needed)

1. **[item 1 · MINOR · arithmetic]** C04's verify floor "≥241 … (230 + 6 + 5 + these 6, C02's 3 not assumed)" — the parenthetical sums to **247**, and every sibling's floor includes its own tests (C01 ≥236, C02 ≥239, C03 ≥235, C05 ≥250). Stated in `C04-kit-environment-dispatch.md` (VERIFY) and repeated at `plan/ledger.md:150`. Harmless as a floor (247 ≥ 241), but internally inconsistent.

2. **[item 5 · MINOR · annotation]** The "116 files scanned" claim (C05 table header, `chunks.md`, ledger) does not reproduce under any plausible `git ls-files` scope: full tree = 237, `src/**/*.st` = 105, `src/` = 106, tree-minus-plan/sources = 122–124. The enumeration's **substance** is independently confirmed correct by my 237-file sweep, so only the count annotation is wrong.

3. **[items 5/6 · MINOR · unaddressed accepted-file schedule]** Accepted `PGRArchSelfTest>>ruledFileAccessSelectors` (`src/Phi-Guardrails-Tests-Core/PGRArchSelfTest.class.st:114–117`) says: *"PCKSrcInventoryCheck joins at M2 by that epic's scheduled edit — not pre-added here."* E11 is that epic, yet no chunk schedules the allowlist edit and no order or cross-epic note mentions it. As cut this is functionally safe — C03's specified walk spellings (`srcPath asFileReference` + message sends only) put no `FileReference`/`FileSystem`/`FileLocator` literal into a production method, so the accepted file-triad arm stays green unamended — but (a) the accepted comment is left promising an edit that never comes, (b) C03's implementer is never told the constraint (any triad literal in the check's production code reds an accepted suite whose fix sits outside every manifest — a latent stop-and-report), and (c) the reflective P-DETERMINISTIC allowlist will not name the gate's second ruled file access that §7.6 declares. Belongs on the cross-epic advisories list / owner's doc pass beside the §0.3 roster note; a one-line addition to C03's digest ("no triad class literal in production code — the accepted `PGRArchSelfTest` triad arm sweeps `Phi-Coding-Kit-Architecture`") would close the hazard.

**Advisory (recorded, not acted on):** the roadmap's "~4 chunks" vs the cut's 5 is explicitly justified in `chunks.md` (two separately reviewable concerns, ~220 LOC/two packages in one diff otherwise) — reasonable and within the 3–10 band.

Scripts and outputs: `/private/tmp/claude-501/-Users-mitt-dev-projects-phi-guardrails/3b7be0ae-3e7e-4deb-9d5e-d0661d42b006/scratchpad/mech.py` (MECHANICAL: PASS, 5 files scanned) and `.../amended.sh` (237 files scanned; all enumerations above). Nothing was changed.

---

*Producer note (appended below the verbatim report, per the method's report-artifact rule): round 1 — PASS on the first validator spawn (fresh isolated subagent, byte-exact {{PACK}} + validation body + EPIC=E11). The three MINORs were swept by the producer in one batch immediately after filing this report (C04/ledger floor arithmetic made consistent; the scan-scope annotation restated as its exact `git ls-files` glob set; C03 digest given the triad-literal constraint line and the stale `PGRArchSelfTest` comment added to the cross-epic advisories for the owner's doc pass). No re-validation required per the punch-list rule.*
