# phi-guardrails — Backlog

*Append-only future items (method operating rule 9). Non-blocking; Prompt 3 must schedule
or knowingly defer each (roadmap disposition list). An item moves onto a stage's decision
sheet only when it starts blocking that stage.*

| Id | Title | Context | Earliest milestone | External consumer / deadline |
|---|---|---|---|---|
| B-01 | Dependency-conformance check | A production package may only reference classes from itself, its declared Metacello baseline dependencies, and a small kernel/platform allowlist — makes declared and actual dependencies agree. D-25 synergy: the baseline is already the trusted inventory. Closes the "loaded, so referenceable" gap the layer map leaves for unmapped packages. | M5 (widening) | — |
| B-02 | Forbidden-reference check with ruling-referenced exceptions | A shipped generic deny-list check (kind `#architecture` for class references; denied *sends* are lint-kind): config maps scope packages → denied classes/packages. Each `#exceptions` entry **requires a decision-log ruling reference** — an exception without one is a configuration error; the report lists every active exception. | early widening | **phi-llm constitution §2.5** (pure-package deny-list) needs it at phi-llm's milestone 0 — if unshipped by then, the client hand-rolls it and we promote later |
| B-03 | Probe: does `RBPackageEnvironment` attribute trait-provided methods to the trait's defining package? | D-15.b probed `CompiledMethod>>package` answering the trait's package; if §2.3's lint environment attributes the same way, a trait defined in an exempt-role package but used from production escapes lint. Probe when the lint check is built (D-15.b agent note, ⟨verify at M1⟩); if confirmed, decide where trait methods get linted. | M1 | — |
| B-04 | Widen lint (and sweeps) over tests-role packages | D-33 ruled tests-role code outside lint's reach as a knowing v1 trade — debug cruft in tests escapes; the revisit rides relocating the lint/secrets bad fixtures from `Tests-*` into the exempt kit-fixtures family (`Phi-Coding-Kit-Fixtures-*` since D-57; D-22's mechanism) in the same stroke. | M5 (widening) | — |
| B-05 | Extension-method attribution in the layer walk | §4.2 known bound: the walk judges methods by defining class, so a foreign package's extensions onto a layer's class are judged as that layer's, and the layer's own extensions on foreign classes go unwalked. Accepted at Gate 2 with "escalate if it bites a real client" — rule 9 routes that standing conditional here. | M5 (or when a client is bitten) | first real client with extension-heavy code (possibly phi-llm) |
| B-06 ~~backlog~~ → **R-46** | `PGRCheckFixturePairMetaRule` — machine-enforce the client fixture-pair convention | **PROMOTED OUT (D-44): not backlog material — ruled v1-widen scope and carried as requirement R-46** (ch. 5 §5.5, ch. 8 §8.1 step 3 — renumbered by D-45). Row retained for the append-only trail; the live item is R-46. Original context: §8.1 step 4 asks clients to give every custom check a fixture pair "by convention" — unenforced, so a client rule that never fires (bad AST pattern, typo in the matcher) reports green, since a rule finding nothing is indistinguishable from a rule with nothing to find. Meta-rule, kind `#behavioral`, registered in `#metaRules` as `behavioral/PGRCheckFixturePairMetaRule`, checkable by naming convention: for every project-scope registered check class `Foo`, a class `FooTest` must exist in a tests-role package. Same shape as ch. 5's M5 "mirror test packages" meta-rule (R-26) and lands with it. | M5 (widening) | — |
| B-07 ~~backlog~~ → **retired** | Abstract CI-adapter test class instead of the copy-paste recipe | **RETIRED (D-45): no adapter exists to abstract** — the invocation model removed the adapter mechanism entirely; clients invoke the gate directly (§7.3) instead of replicating a test. Row retained for the append-only trail (B-06 precedent). Original context: §8.1 step 5 told each client to replicate the adapter test; copy-pasted setup drifts when the framework later changes how the gate is invoked, with no way to detect it. Ship an abstract `TestCase` the client subclasses, implementing one method (its artifact path) and inheriting the assertion and report formatting. **Declined for v1 with reason** (ch. 8 review, finding 3): the only clean home is a new package — SUnit must not enter `Phi-Guardrails-Gate` (D-10 keeps SUnit at the edge) and the adapter is kit-neutral, so `-Coding-Behavioral` would be a category error — which is structural cost for a v1 with two adopters and zero measurable drift. Land it with §8.4's external-adoption proof, when the second real adapter copy exists. | §8.4 external-adoption proof (or M5, whichever first) | the external toy copy is the first genuine second copy |
| B-08 | External-kit proof | Build a trivial kit outside this repo against `Phi-Guardrails-SDK` only and compose it into a project — proves the kit contract crosses repos and makes the first-kit stance (D-56 ruling 2: the coding kit is resident for convenience, privileged in nothing) testable; the decision point for extracting the coding kit to its own repository. | widening (beside §8.4's external-adoption proof) | — |
| B-09 | Naming-boundary check | D-56 rider (b), routed here (agent call, veto-open): a widened-catalog candidate enforcing the prefix law as the layer map enforces references — framework packages contain only `PGR*` classes, kit packages only their kit's prefix (coding kit `PCK*`, regex-disjoint from `PGR`), toy packages `Toy*`. | M5 (widened catalog) | — |
| B-10 ~~backlog~~ → **D-66** | Pin the D-31.a toolchain downloads by checksum (or archived copy) | **RULED accepted (D-66, owner notice 2026-07-23): pin the build actually installed and re-verified green (`4f7563dfe5`); `tools/install.sh` fails loudly on mismatch; archived copy remains open as the stronger later form. Cut as a small chunk in E02's Prompt-4 run.** Row retained for the append-only trail. Original context: C01 completion report + review (E01, 2026-07-23): the `get-files/130` channel demonstrably drifts — it served `4f7563dfe5` (a 13.1-series snapshot under the Pharo13.0 name), not the `4c3e4714cc` build the D-15/D-25.a spelling inventory was verified on; all relied-on spellings re-executed green (D-63 risk line). | E02 (ruled) | — |
| B-11 | Tonel metadata files (`.properties`/`.project`) if a loader path demands them | C02 observation (E01, 2026-07-23): bare Tonel (`src/` with no `.properties`) satisfied Metacello's `tonel://` loader. **Resolved during E01:** the smalltalkCI `filetree://` path did require it (CI run 30066695778); `src/.properties` `{ #format : #tonel }` landed via C02 reopen (`afb44e8`), recorded in D-63. Row retained for the append-only trail. | ~~E01/C03–C04~~ resolved | — |
| B-12 ~~backlog~~ → **D-67** | Machine-enforced chunk commit hygiene (M0 mining repeat) | **RULED accepted (D-67, owner notice 2026-07-23): the `tools/` precondition check — clean tree (modulo the ledger) before an implementer spawns; each accepted chunk committed before the next pick — cut as a small E02 chunk; from E02 on, work orders state commit expectations explicitly.** Row retained for the append-only trail. Original context: E01 milestone mining (2026-07-23), the one correction that repeated across chunks: uncommitted working-tree state kept leaking between chunk boundaries — C01 left all deliverables uncommitted; C02's commit had to carry C01's decision-log text and filed a who-commits question; the orchestrator needed a bookkeeping commit before C03 published; C03 and C04 both had to disclaim the orchestrator's uncommitted ledger edit in their reports. | E02 (ruled) | — |
